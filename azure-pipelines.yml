trigger:
  branches:
    include:
    - master
    - releases/*
  paths:
    include:
    - src/.version

pool:
  vmImage: 'windows-latest'

steps:

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -BuildAndTest   
  displayName: 'Build and Test'

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -PackAndPublish -nugetApiKey oy2echmcjgq74szblp6qqlqvxsclsuzu3bz4i4mkjwe7cu   
  displayName: 'Pack and Publish'
