trigger:
  branches:
    include:
    - master
    - releases/*
  paths:
    include:
    - src/.version

pool:
  vmImage: 'windows-latest'

steps:

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -BuildAndTest   
  displayName: 'Build and Test'

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -PackAndPublish -nugetApiKey oy2ezxdj7cu4gp6yuocqnq6q3dtllveupbtve43vrrpg6u   
  displayName: 'Pack and Publish'
